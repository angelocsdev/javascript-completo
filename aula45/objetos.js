"use strict"

class Pessoa {
    constructor() {
        this.nome = pnome
    }
}

const p1=new Pessoa("João") // se o construtor não receber parâmetro, a class tbm não recebe
const p2=new Pessoa("Psirico")
const p3 = new Pessoa("Josilda")


console.log(p1.nome)
console.log(p2.nome)
console.log(p3.nome)